# react-simple-maps has an unresolved peer-dependency conflict with React 19,
# so a plain `npm install` fails. Setting it here means CI and Vercel builds
# work without anyone having to remember the flag.
legacy-peer-deps=true
